#include "main.h"

/**
  *add - adds twi integers and returns the result
  *@a: first integer operand
  *@b: second integer operand
  *
  *Return: addition result of integer type
 */
int add(int a, int b)
{
	return ((a + b));
}
