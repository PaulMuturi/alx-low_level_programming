#include "main.h"
#include <string.h>

/**
  *_strlen - returns the length of a string
  *@s:string
  *
  *Return: integer , string length
  */

int _strlen(char *s)
{
	int strlength = strlen(s);

	return (strlength);
}
